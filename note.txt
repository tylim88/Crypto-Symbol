future works

- sync daily (but only publish if found a new symbol)
- add sync function (only work in server environment due to cors issue)
- add mechanics to detect and handle duplicated symbols (impractical, only one edge case currently)
